<%= form_with(model: action_card) do |form| %>
  <% if action_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(action_card.errors.count, "error") %> prohibited this action_card from being saved:</h2>

      <ul>
        <% action_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
